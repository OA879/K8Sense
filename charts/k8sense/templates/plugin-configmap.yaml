{{- if .Values.pluginsManager.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "k8sense.fullname" . }}-plugin-config
  namespace: {{ include "k8sense.namespace" . }}
  labels:
    {{- include "k8sense.labels" . | nindent 4 }}
data:
  plugin.yml: |{{ .Values.pluginsManager.configContent | nindent 4 }}
{{- end }}
