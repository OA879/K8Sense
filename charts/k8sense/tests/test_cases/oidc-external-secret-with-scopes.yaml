# This is a test case for OIDC external secret with hasScopes enabled.
# When hasScopes is true, the -oidc-scopes argument is included so that
# the OIDC_SCOPES key from the external secret is passed to K8sense.
config:
  oidc:
    secret:
      create: false
    externalSecret:
      enabled: true
      name: oidc
      hasScopes: true
