# This is a test case for volumes in the K8sense deployment.
# The volumes field is a list of dictionaries that specify the volumes to add to the K8sense deployment.
volumes:
  - name: plugins
    emptyDir: {}
