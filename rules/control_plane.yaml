# CP-* rules — see backend/pkg/clusterdoctor/checks/control_plane.go.
# These target self-hosted / kubeadm / kind control planes where the
# components run as static pods in kube-system. On managed control planes
# (EKS/GKE/AKS) the components aren't visible as pods, so these checks simply
# find nothing — which is the correct result there.
- id: CP-002
  name: Control Plane Component Not Ready
  severity: CRITICAL
  category: control_plane
  check_fn: check_control_plane_pod_not_ready
  description: >
    A control-plane component pod (etcd, kube-apiserver, kube-scheduler, or
    kube-controller-manager) is not in a Ready state. This can degrade or
    halt cluster operations.
  remediation: |
    1. Identify the unhealthy component:
       kubectl get pods -n kube-system -l tier=control-plane -o wide
    2. Inspect it:
       kubectl describe pod <pod-name> -n kube-system
       kubectl logs <pod-name> -n kube-system
    3. For etcd specifically, check disk health and member list — do NOT
       delete or restart etcd pods without a backup and a maintenance window.
  guided_fix:
    action: ""
    warning: ""
  references:
    - "https://kubernetes.io/docs/tasks/debug/debug-cluster/"

- id: CP-005
  name: Control Plane Pod Restarted Recently
  severity: WARNING
  category: control_plane
  check_fn: check_control_plane_pod_restarted
  description: >
    A control-plane component pod restarted within the last hour. An
    occasional restart may be benign (e.g. a node reboot), but repeated
    restarts point to resource pressure, disk latency, or a crash loop.
  remediation: |
    1. Check restart history and the last termination reason:
       kubectl describe pod <pod-name> -n kube-system
    2. Correlate with node conditions (NODE-003/004) and etcd disk latency.
    3. If restarts continue, gather logs before the restart:
       kubectl logs <pod-name> -n kube-system --previous
  guided_fix:
    action: ""
    warning: ""
  references:
    - "https://kubernetes.io/docs/tasks/debug/debug-cluster/"

- id: CP-003
  name: Scheduler Not Leader-Elected
  severity: CRITICAL
  category: control_plane
  check_fn: check_scheduler_not_leader
  description: >
    The kube-scheduler leader Lease has no fresh holder. With no active
    scheduler, newly-created pods will stay Pending indefinitely.
  remediation: |
    1. Check the scheduler lease:
       kubectl get lease kube-scheduler -n kube-system -o yaml
    2. Inspect the scheduler pods/logs:
       kubectl -n kube-system get pods -l component=kube-scheduler
       kubectl -n kube-system logs -l component=kube-scheduler --tail=100
    3. On managed control planes this is handled by the provider — verify via
       the provider console.
  guided_fix: { action: "", warning: "" }
  references:
    - "https://kubernetes.io/docs/concepts/scheduling-eviction/kube-scheduler/"

- id: CP-004
  name: Controller-Manager Not Leader-Elected
  severity: CRITICAL
  category: control_plane
  check_fn: check_controller_manager_not_leader
  description: >
    The kube-controller-manager leader Lease has no fresh holder. Core control
    loops (replicaset, node, endpoint, etc.) are not running.
  remediation: |
    1. Check the lease:
       kubectl get lease kube-controller-manager -n kube-system -o yaml
    2. Inspect the controller-manager pods/logs:
       kubectl -n kube-system logs -l component=kube-controller-manager --tail=100
  guided_fix: { action: "", warning: "" }
  references:
    - "https://kubernetes.io/docs/reference/command-line-tools-reference/kube-controller-manager/"

- id: CP-001
  name: API Server Health Degraded
  severity: CRITICAL
  category: control_plane
  check_fn: check_apiserver_readyz_degraded
  description: >
    The API server's /readyz endpoint reports one or more failing subsystems
    (etcd, informer-sync, post-start hooks, ...). The API server is reachable
    but not fully healthy, which can cause intermittent request failures.
  remediation: |
    1. See the full readiness report:
       kubectl get --raw='/readyz?verbose'
    2. Investigate the specific check(s) listed with a [-] prefix.
    3. Common causes: etcd unreachable/slow, an unfinished post-start hook, or
       informers not yet synced after a restart. Check the apiserver logs:
       kubectl -n kube-system logs -l component=kube-apiserver --tail=100
  guided_fix: { action: "", warning: "" }
  references:
    - "https://kubernetes.io/docs/reference/using-api/health-checks/"

- id: CP-006
  name: etcd Responding Slowly
  severity: WARNING
  category: control_plane
  check_fn: check_etcd_slow
  description: >
    More than 1% of etcd requests, as observed by the API server, are taking
    longer than 1s. Slow etcd (usually disk I/O or network) degrades every
    write and leader election across the cluster.
  remediation: |
    1. Confirm from the API server's perspective:
       kubectl get --raw=/metrics | grep etcd_request_duration_seconds
    2. Check etcd disk latency (fsync) and CPU on control-plane nodes; etcd
       needs low-latency SSD storage.
    3. Verify network round-trip between the API server and etcd members.
  guided_fix: { action: "", warning: "" }
  references:
    - "https://etcd.io/docs/latest/op-guide/hardware/"

- id: CP-007
  name: API Server High Error Rate
  severity: WARNING
  category: control_plane
  check_fn: check_apiserver_error_rate
  description: >
    Over 5% of the API server's responses are 5xx server errors. This points
    to an overloaded or failing control plane (etcd problems, failing
    admission webhooks, or resource exhaustion).
  remediation: |
    1. Break errors down by resource and verb:
       kubectl get --raw=/metrics | grep 'apiserver_request_total{.*code="5'
    2. Check for failing or slow admission / conversion webhooks.
    3. Inspect API server logs and node resource pressure:
       kubectl -n kube-system logs -l component=kube-apiserver --tail=100
  guided_fix: { action: "", warning: "" }
  references:
    - "https://kubernetes.io/docs/concepts/cluster-administration/monitoring/"
